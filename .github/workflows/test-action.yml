name: Test Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2001937@ds.study.iitm.ac.in
        run: echo "Hello, world!"
